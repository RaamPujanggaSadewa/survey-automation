# Synthesis

Human-readable findings per case, named `<case-name>.md`. An AI writes this
from `responses/<case-name>.csv` (or `.json`) plus the original case file —
key findings, quotes, and recommendations — before that gets turned into
`specs/<case-name>.slides.gs` for the actual deck.
