/**
 * Placeholder. scripts/generate-slides.sh overwrites this file with the
 * contents of specs/<case-name>.slides.gs before pushing to Apps Script.
 */
var SLIDES_SPEC = {
  title: 'No case loaded',
  subtitle: 'Run scripts/generate-slides.sh <case-name> to load a spec.',
  sections: []
};
