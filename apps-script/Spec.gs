/**
 * Placeholder. scripts/generate-form.sh overwrites this file with the
 * contents of specs/<case-name>.spec.gs before pushing to Apps Script.
 */
var SURVEY_SPEC = {
  title: 'No case loaded',
  description: 'Run scripts/generate-form.sh <case-name> to load a spec.',
  questions: []
};
